#!/bin/bash
# Custom Ansible Facts
# Managed by Ansible - Do not edit manually
# {{ ansible_managed }}

cat << EOF
{
  "project": "{{ project_name | default('chikwex-infra') }}",
  "environment": "{{ environment_name | default('unknown') }}",
  "managed_by": "ansible",
  "last_run": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "ansible_version": "{{ ansible_version.full | default('unknown') }}",
  "role_versions": {
    "common": "1.0.0"
  }
}
EOF
